{% if params %}
aaa group server radius {{ params.name }}
{%   for server in params.servers %}
 server name {{ server.name }}
{%   endfor %}
{%   if params.vrf %}
 ip vrf forwarding {{ params.vrf }}
{%   endif %}
{%   if params.src_interface %}
 ip radius source-interface {{ params.src_interface }}
{%   endif %}
{% endif %}